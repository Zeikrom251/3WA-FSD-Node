extends layout.pug

block content
  .container
    h1 Contact
    form(method="POST" action="/contact-me")
      label(for="email") Email
      input#email(type="email" name="email" required)
      label(for="message") Message
      textarea#message(name="message" required)
      button(type="submit") Send

block scripts
  //- Ajoutez ici des scripts spécifiques à la page contact si besoin
